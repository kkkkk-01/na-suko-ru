;; ==========================================================
;; HTTP/WebSocket Configuration for WebRTC
;; ==========================================================

[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
prefix=
enablestatic=no

; CORS設定（開発環境用）
; 本番ではドメインを限定すること
